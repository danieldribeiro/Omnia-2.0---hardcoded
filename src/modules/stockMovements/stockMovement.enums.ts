export enum MovementType {
  ENTRY = "ENTRY",
  EXIT = "EXIT"
}

export enum MovementUnit {
  UNIT = "UNIT",
  PACK = "PACK"
}

export enum MovementReason {
  SELL = "SELL",
  BUY = "BUY",
  LOSS = "LOSS",
  ADJUST = "ADJUST",
  CONSUME = "CONSUME"
}